# Paper reference outputs

These are the final numerical arrays and PDF figures used by the manuscript.

They are reference outputs only. The Rust `reproduce` command recomputes the
experiments from `reference_inputs/` and does not read these arrays.

Use `cargo run --release -- validate` after a recomputation to compare a Rust
run with these paper outputs.
